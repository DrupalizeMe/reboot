/**
 * @file
 * JavaScript for button component.
 */

(function ($, window, Drupal) {
  Drupal.behaviors.rebootButton = {
    attach: function attach(context, settings) {

      console.log('Button component JS loaded');

    }
  }
})(jQuery, window, Drupal);
